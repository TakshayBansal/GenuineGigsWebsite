import type { Metadata } from "next";
import { CheckCircle2 } from "lucide-react";
import { DemoForm } from "@/components/DemoForm";

export const metadata: Metadata = { title: "Book a Manufacturing Workflow Demo", description: "Bring one real planning, procurement, supplier, material, or production recovery workflow to a tailored GenuineGigs demo.", alternates:{canonical:"/demo"} };
export default function DemoPage() { return <section className="section demo-page"><div className="shell demo-layout"><div className="demo-copy"><p className="kicker">Book a tailored demo</p><h1>Bring one real planning or recovery workflow.</h1><p>Bring a recurring material shortage, supplier disruption, planning problem, procurement workflow, or production recovery case. We will map the evidence, calculate the exposure, compare practical responses, and show how the decision moves through governed execution and verification.</p><div className="demo-points"><div className="demo-point"><CheckCircle2 size={18}/>Your current evidence and planning view</div><div className="demo-point"><CheckCircle2 size={18}/>Practical response choices and required authority</div><div className="demo-point"><CheckCircle2 size={18}/>A path from approved action to verified outcome</div></div></div><DemoForm/></div></section>; }
